.page {
  min-height: 100vh;
  background: var(--bg);
  padding: 24rpx;
  box-sizing: border-box;
}

.card {
  padding: 28rpx;
}

.title {
  font-size: 32rpx;
  font-weight: 900;
  display: block;
}

.body {
  margin-top: 16rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
  line-height: 1.6;
  display: block;
}
